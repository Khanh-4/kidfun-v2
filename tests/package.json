{
  "name": "kidfun-e2e-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for KidFun parent dashboard",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:auth": "playwright test auth/",
    "test:profiles": "playwright test profiles/",
    "test:devices": "playwright test devices/",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
